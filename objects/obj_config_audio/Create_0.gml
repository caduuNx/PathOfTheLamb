// Tamanho dos sliders
slider_w = 300;
slider_h = 20;

// Centro da tela GUI
var center_x = display_get_gui_width() / 2;

// Posições centralizadas
slider_sfx_x = center_x - slider_w / 2;
slider_mus_x = center_x - slider_w / 2;

// Posição vertical ajustada (mais pra baixo)
slider_sfx_y = 310; 
slider_mus_y = 440; 
